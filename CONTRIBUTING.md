## مشارکت (Contributing)

اگر دوست دارید به توسعه این پروژه کمک کنید،
کافیه پروژه رو فورک کنید، تغییرات رو اعمال کنید و در نهایت یک Pull Request بفرستید.

اگر باگ، مشکل یا ایده‌ای برای قابلیت جدید دارید، لطفاً از طریق [issues](https://github.com/MahdyarEn/samad-sharif/issues) گیت‌هاب مطرحش کنید.

---

### پیش‌نیازها

برای اجرای درست پروژه، موارد زیر لازم است:

- **Node.js:** نسخه 18 یا بالاتر
- **npm:** نسخه 9 یا بالاتر
  > پروژه به‌صورت `ESM` نوشته شده (`"type": "module"`)، بنابراین اجرای آن با Node قدیمی‌تر پشتیبانی نمی‌شود.

---

### ساختار پوشه‌ها (Folder Structure)

- `public` → مربوط به **MiniApp (WebApp تلگرام)**
- سایر فایل‌ها و پوشه‌ها → مربوط به منطق اصلی ربات تلگرام و سرور

---

### شروع کار

- روی دکمه **Fork** (بالا سمت راست) کلیک کنید
- ریپوی فورک‌شده را کلون کنید
- وابستگی‌ها را نصب کنید:

```bash
npm install
```

اجرای پروژه در حالت توسعه (Node 18+):

```bash
node --watch index.js
```

### ارسال تغییرات

تغییرات خود را در ریپوی فورک‌شده Commit کنید
سپس یک Pull Request به این مخزن ارسال کنید.
